# Testing Matrix

## Wajib
- `npm run lint`
- `npm run test`
- `npm run build`

## Targeted Tests
- Tambah test untuk routing mode (normal/websearch).
- Tambah test untuk tools calling (schema + behavior).
- Tambah test untuk fallback provider + citations.

## Manual Checks
- Coba chat normal dan websearch.
- Verifikasi UI streaming tidak rusak.
- Cek paper mode stage update.
