# Scan Guide (Read-Only)

Use the scan script first, then validate findings manually.

## Default targets
- src/**
- backend/** (if exists)
- convex/**
- package.json

## What to capture
- Entry points, routing, and public API surface.
- Data models, schemas, and validation.
- External integrations and environment variables.
- Areas that influence documentation claims.
