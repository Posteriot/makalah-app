# App
APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Convex
NEXT_PUBLIC_CONVEX_URL=""
CONVEX_DEPLOYMENT=""

# Clerk (Auth) — commented out, kept for rollback reference
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
# CLERK_SECRET_KEY=""
# CLERK_WEBHOOK_SECRET=""
# NEXT_PUBLIC_CLERK_SOCIAL_PROVIDERS="google"

# BetterAuth
BETTER_AUTH_SECRET=
SITE_URL=http://localhost:3000
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXT_PUBLIC_CONVEX_SITE_URL=

# Xendit (Payments)
XENDIT_SECRET_KEY=""
# Optional: public key and webhook secret/callback token
XENDIT_PUBLIC_KEY=""
XENDIT_WEBHOOK_SECRET=""
XENDIT_CALLBACK_TOKEN=""

# Resend (Email)
RESEND_API_KEY=""
RESEND_FROM_EMAIL="no-reply@makalah.app"

# AI / LLM via Vercel AI Gateway
AI_GATEWAY_URL=""
# Use one of these depending on Vercel docs/config
AI_GATEWAY_API_KEY=""
VERCEL_AI_GATEWAY_TOKEN=""

# OpenRouter (fallback AI provider)
OPENROUTER_API_KEY=""

# AI Provider Configuration Encryption
# Required for encrypting API keys in database (admin-managed provider configs)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=""

# Optional: direct provider keys (only if calling providers directly, not via Gateway)
OPENAI_API_KEY=""
GOOGLE_GENERATIVE_AI_API_KEY=""

# GitHub (local-only, do NOT commit real values)
# Repository URL (safe to commit if needed)
GITHUB_REPO_URL="https://github.com/Posteriot/makalah-app.git"

# Auth tokens: placeholders only; do not put actual tokens
# into any file that might be committed or shared.
GITHUB_FINE_GRAINED_PAT=""
GITHUB_CLASSIC_PAT=""
