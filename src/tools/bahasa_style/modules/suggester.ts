// Currently suggestions are mapped statically in definitions.ts
// This module is prepared for advanced suggestions (e.g., Synonym rotation logic) if needed later.

export class Suggester {
    static getSynonym(word: string): string | null {
        // Placeholder for synonym logic
        return null;
    }
}
